// This module is intentionally empty.
// It's used as a Webpack alias for Node.js built-in modules
// (especially those with the 'node:' prefix) that are not
// available or needed in the browser environment, and for which
// 'resolve.fallback: false' might not be sufficient due to
// issues with Webpack handling the 'node:' scheme.
module.exports = {};
